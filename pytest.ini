[pytest]
markers =
    integration: end-to-end tests that exercise multiple subsystems (slow)
    db: tests that require a TimescaleDB/Postgres backend (opt-in via `pytest -m db`)
    live_broker: tests that exercise live-broker adapters via mocked SDK (CI may skip)
# Default test run excludes integration tests — use `make test-integration`
# or `pytest -m integration` to opt in.
addopts = -m "not integration and not db"
